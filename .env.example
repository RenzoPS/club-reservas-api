# Copiar este archivo a .env y ajustar si hace falta:
#   cp .env.example .env
# El .env NO se commitea (esta en .gitignore).

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=club
